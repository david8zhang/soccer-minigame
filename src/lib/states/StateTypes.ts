export enum PlayerStates {
  PLAYER_CONTROL = 'PlayerControl',
  WAIT = 'Wait',
  SUPPORT = 'Support',
  DRIBBLE = 'Dribble',
}

export enum TeamStates {
  ATTACKING = 'Attacking',
  DEFENDING = 'Defending',
}
